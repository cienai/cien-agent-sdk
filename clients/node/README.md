# cien-agent-sdk (Node.js)

Node.js client scaffold. Implementation is intentionally pending.

Planned package location:

- `clients/node/src`

The Python client in `clients/python` is the reference implementation.
